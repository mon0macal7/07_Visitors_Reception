//Aquí estara el fetch y el export de admin.js
const guardarVisitas = document.getElementById('btn-siguiente')
guardarVisitas.addEventListener('submit', e => {
    e.preventDefault();
console.log('submit');

})
